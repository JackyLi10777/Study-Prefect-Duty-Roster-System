-r requirements.txt

# Release verification only. The daily local application does not require
# these packages, but a replacement computer needs them before running the
# isolated browser, PDF-inspection, and automated regression evidence.
pytest>=9.0,<10
playwright>=1.58,<2
pypdf>=6.15,<7
httpx2>=2.5,<3
fonttools>=4.63,<5
pip-tools>=7.5,<8
pip-audit>=2.10,<3
bandit>=1.9,<2
detect-secrets>=1.5,<2
