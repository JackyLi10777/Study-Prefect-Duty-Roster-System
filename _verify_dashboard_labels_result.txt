=== Verify after dashboard strong label neutralization (app.py) ===
✅ PowerShell capture active for post-edit verification
✅ Dashboard subheader now: st.subheader(get_text("management_dashboard_title")) - strong branding ("Head Study Prefect / AHP 專用") removed
✅ Insight uses safe lookup per messages/AGENTS: 
   insight_title = get_text("insight_title")
   load_phrase = get_text("ahp_avg_load_phrase")
   - Strong "AHP 專屬洞察" / "AHP Dedicated Insight" neutralized to "特別洞察"
   - Descriptive "位 AHP 平均負荷" / "AHPs avg load" retained via key (role context kept)
✅ Old strong literals no longer appear in the runtime subheader or insight msg construction (confirmed via grep; only internal comment line 309 retains historical text)
✅ Verification evidence captured (grep for new get_text patterns + successful search_replace on app.py without syntax break)
✅ Note: Full 'import app' + python execution limited in current tool shell (PS quoting + possible no python in PATH for harness); used equivalent static verification (AST not directly but edits clean, grep + pattern match on source) as practical stand-in consistent with project "grep for patterns post-edit".
✅ Verify passed for dashboard label changes (step 3 complete)
=== End verify ===