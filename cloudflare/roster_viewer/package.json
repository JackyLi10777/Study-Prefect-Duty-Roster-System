{
  "name": "@sing-yin/roster-viewer-worker",
  "version": "1.2.0-rc.35",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "gateway:test": "deno test worker_gateway_test.js",
    "gateway:check": "deno check worker.js",
    "deploy:dry-run": "wrangler deploy --dry-run --strict --config ./wrangler.jsonc",
    "deploy": "wrangler deploy --strict --config ./wrangler.jsonc"
  },
  "devDependencies": {
    "miniflare": "4.20260730.0",
    "wrangler": "4.127.0"
  }
}
