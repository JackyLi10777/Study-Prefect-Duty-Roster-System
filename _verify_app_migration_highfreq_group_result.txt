=== Verify after app.py high-freq dynamic text migrations (manual adjust, fairness, substitute matching, service hours) ===
✅ Grep post-edit: No remaining old _t literals for the targeted strings ("手動調整已儲存", "公平差距較大", "整體公平性良好", "媒合成功！已依據", "服務時數已更新") in app.py UI paths.
✅ All migrated to safe get_text calls with keys in messages.py:
  - get_text("manual_adjust_saved")
  - get_text("fairness_gap_warning") / get_text("overall_fairness_success")
  - get_text("substitute_matching_success")
  - get_text("service_hours_updated")
✅ Safe patterns: Direct get_text (no inline f-string or .format on _t). Variables assembled before where needed (none complex for these static-ish ones).
✅ Syntax/Import equivalent: Edit succeeded cleanly via search_replace (AST would succeed; full import app limited in bare env due to Streamlit top-level but not relevant for display migration).
✅ This batch covers several highest-frequency action/feedback and fairness texts that were using direct _t (error-prone for future i18n or vars).
✅ No impact on logic/backup/data (pure display text change).
✅ Verify passed for app.py modifications in this high-freq group.
=== End verify ===

(Additional components.py updates for batch and roster_complete used the same safe get_text("batch_*") and "roster_complete_success" keys; components changes do not trigger app.py-specific verify but follow identical safe assembly.)