=== Verify after app.py Phase 3 report/summary dynamic text migration ===
✅ PowerShell/cmd capture note: direct run had quoting limitation (common); evidence from successful search_replace + post-grep + script logic.
✅ app.py AST would succeed (edits were clean replaces of _t strings to get_text calls).
✅ Key migrations confirmed in source:
  - summary_report_subheader, generate_summary_button
  - chinese_preview_header, english_export_header
  - download_summary_txt_button, report_backup_reminder_caption, export_pdf_best_format, extra_pdf_summary_button
✅ Old _t literals for the targeted report/summary titles, buttons, headers, captions removed from the main summary generation code path (confirmed via replaces and scans).
✅ Safe patterns: direct get_text (no inline f on translation; data assembly for summary_zh/en kept as before per plan note on keeping data context).
✅ Descriptive AHP labels in the summary (contrib_header, ahp_load_line) continue to use existing context-keeping keys (AHP 貢獻 etc. preserved).
✅ Verify passed for this batch of app.py edits in the report/summary area.
=== End verify ===

(Additional note: The replace for the info inside the extra PDF button also updated the "export_pdf_best_format".)