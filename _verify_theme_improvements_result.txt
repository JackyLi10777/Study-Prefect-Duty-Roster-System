=== Theme CSS Verification (Dark Mode contrast + smoother + enclosure preservation) ===
✅ --accent-gold: #D4AF37 in base (preserved)
✅ 3px solid var(--accent-gold) in base
✅ Verse padding vars present in base
✅ .verse-inner nesting + padding in base
✅ Dark mode has overflow hidden for enclosure
✅ Dark overrides 3px gold border (!imp, preserves enclosure)
✅ Dark preserves padding vars !imp
✅ Light preserves 3px gold border
✅ HC mode untouched (still uses --hc-gold, has structure)
✅ HC verse enclosure structure preserved (different gold but same 3px/padding/inner)
✅ Improved --dark-text-secondary (brighter #f3f4f6 for captions/labels/secondary)
✅ Dedicated verse/reflection contrast vars added
✅ Dark .verse-text now uses brighter --dark-verse-text
✅ Dark reflection uses brighter --dark-reflection-text
✅ Reflection * specificity / brighter text for contained content contrast
✅ Smooth transitions added in base (for verse-card, sidebar, app, captions etc)
✅ Placeholders use (improved) dark secondary
✅ Captions use improved dark secondary

✅ VERIFICATION PASSED: Dark contrast improved (verse/reflection/secondary/captions/placeholders via brighter vars + targeted rules), enclosure + gold #D4AF37 + paddings + .verse-inner + overflow + 3px borders 100% preserved exactly in base/dark/light/HC (no value or structure changes), transitions for smoother switching added in base, HC untouched, only edited roster/ui/theme.py with small focused changes (3 replaces total).

No changes to:
- verse enclosure HTML/structure/padding values (in components.py untouched)
- gold accent colors
- layout/behavior
- High Contrast logic/CSS

Result: noticeably better dark readability for the listed elements + fluid mode switch feel. Enclosure stable.