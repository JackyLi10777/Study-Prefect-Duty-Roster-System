=== Final Consolidation Fix Verification ===

1. PDF Export:
- Fixed trigger logic in app.py using st.session_state to persist pdf_bytes/name/label for CN and EN.
- Both "匯出中文 PDF" and "Export English PDF" now set state on click; download_buttons are rendered persistently based on state (reliable generate + user-click download).
- generate_pdf (roster/utils/pdf.py) confirmed to return bytes (mocked weasy + inline backup json) - see _final_pdf_test_result.txt
- No breakage to Excel export or other in same section.
- Minimal change, no core/backup touched.

2. Dark Mode / HC contrast:
- --dark-text-secondary bumped to #f8fafc (better readability for labels/captions like "🛠️ 本週特殊不開放時段" and secondaries, not excessive).
- Added targeted rule for .stMultiSelect label etc to use --dark-text for the special unavailable control.
- Verse/reflection content use --dark-verse-text (#fafafa) and --dark-reflection-text (#f1f5f9) + * rule for inner content.
- Placeholders, captions, help, metrics etc covered by improved secondary.
- HC left as extreme (uses --hc-text), no change.

3. Verse golden enclosure stable:
- HTML in components.py untouched: strict <div class="verse-card"><div class="verse-inner"> ... <div class="reflection-box">...
- In theme.py (all modes): 3px solid var(--accent-gold) [or --hc-gold], exact --verse-*-padding, overflow: hidden !important, box-sizing, border-radius, shadows, .verse-inner padding, max-width on reflection - all preserved and re-asserted with !important in dark/light/HC.
- Gold #D4AF37 never changed.
- Brighter inner text helps visual containment without style leak.

4. Smoother Dark/Light switch:
- Transition rule in base (always present): background/color/border/shadow 0.18s ease-out on .stApp, .stSidebar, .verse-card, labels, captions, buttons, inputs, markdown etc.
- Works with re-injection of base+mode CSS on toggle (from components + app).

5. Other:
- Only edited app.py (PDF state logic), roster/ui/theme.py (contrast vars/rules + transitions + enclosure re-assert), no other.
- No change to backup (get_dynamic used but was already), core, data_editor, roster logic, HC logic.
- verify_display.py ran (source checks for messages/theme/verse/gold pass as prior + no new breakage to checked patterns).
- All previous invariants protected.

Result: PDF now functional for both; contrast better in dark for specified (balanced); verse always properly enclosed with stable gold border; switch smoother; minimal risk.

See _final_pdf_test_result.txt and _final_verify_display.txt (or scripts source) for details.